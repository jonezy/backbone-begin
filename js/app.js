define([
  'jquery',
  'underscore',
  'backbone'
], function($, _, Backbone) {
  var app = window.BackboneBegin = {};
  app.Collections = {};
  app.Models = {};
  app.Views = {};
  app.Version = '0.0.0';

  return app;
});
